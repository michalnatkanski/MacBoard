import styles from '../AppBoard/AppBoard.module.scss';

const Appboard = () => {
    return (
      <div className={styles.wrapper}>
          App board
      </div>
    );
  }
  
  export default Appboard;
  